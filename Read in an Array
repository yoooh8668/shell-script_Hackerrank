i=0
while read X
do
    READ[$i]=$X
    i=$i+1
done

echo ${READ[@]}

# 'READ[$d]'  set $d'th of READ

# '${#READ[@]}' read array size of READ
# '${READ[@]}'  read array all element of READ
# '${READ[$d]}' read array the $d'th element of READ
